export const metadata = {
  title: "Mi cuenta",
  robots: {
    index: false,
    follow: false,
    noarchive: true,
  },
};

export default function AccountLayout({ children }) {
  return children;
}
